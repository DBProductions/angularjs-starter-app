language: node_js
node_js:
  - 0.10

before_install:
  - npm install -g grunt-cli

before_script:
  - npm start > /dev/null &
  - npm run webdriver > /dev/null &
  - sleep 1

script:
  - npm run protractor