language: node_js
node_js:
  - 0.10

before_install:
  - npm install -g grunt-cli
  - npm install -g protractor

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - npm start > /dev/null &
  - npm run webdriver > /dev/null &
  - sleep 1

script:
  - npm run protractor